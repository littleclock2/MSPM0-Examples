此工程使用IO中断实现矩阵键盘
理论上中断服务函数中代码要尽量精简并且避免delay，但是要结合实际情况考虑
为了避免误判，一些延迟消抖是必要的
除了使用二分法确定按键所在行以外，还可以使用之前的扫描法，二者时间复杂度差不多
除了将二分法（或扫描法）放在IO中断服务函数中，还可以设置标志位在while(1)中运行，运行后标志位清零
但如果while(1)里代码运行时间过长，或者delay函数过多，可能会导致中断发生的时间与检测具体按键的时间相差很多，从而无法正确检测
可以尝试注释一些代码观察现象